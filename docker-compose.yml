services:
  frontend:
    build:
      context: .
      args:
        RUN_TESTS_ON_BUILD: ${RUN_TESTS_ON_BUILD:-true}
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
