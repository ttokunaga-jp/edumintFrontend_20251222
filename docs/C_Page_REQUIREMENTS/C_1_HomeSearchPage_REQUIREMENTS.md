# C_1 HomeSearch Page REQUIREMENTS

## 機能要件
- 画面構成（Figma/実装ガイド準拠）
  - TopMenuBar（検索バー/通知/ユーザー）
  - Hero（価値提案 + 「問題を作成」CTA）
  - AdvancedSearchPanel（折りたたみ。フィルター指定時は自動展開）
  - ContextHealthAlert（Alert A/B）
  - SearchResultCard 一覧（grid）
  - Pagination
- 検索
  - 検索クエリは TopMenuBar の検索バーと同期（Home内の検索入力は重複させない）
  - ソート: おすすめ / 最新 / 人気 / 閲覧数（実装は `sortBy` に集約）
  - 検索結果 0 件は EmptyState を表示
- フィルター（代表例）
  - keyword, universities[], faculties[], subjects[]
  - fieldType, level, formats[], duration, period, sortBy
- API
  - `GET /search/exams`
  - `GET /search/readings`（読み/多言語サジェスト）
- Health
  - `GET /health/search`: `outage|maintenance` で入力/CTA disable（API呼び出しも抑止）
  - `degraded` は警告表示（CTAは原則有効）
- モック（暫定）
  - 現状 `src/src/services/api/gateway.ts` は `VITE_API_BASE_URL` が localhost の場合にモックへ分岐。

## 非機能要件
- TTI p75 < 2.5s。検索 API 503 時はリトライ1回＋Alert。
- i18n 辞書必須。共通コンポーネントのみ参照。
- レスポンシブ: md(768px) で検索/メニューの配置を切替。

## 画面/コンポーネント配置
- tsx: `src/src/pages/HomePage.tsx`（FIGMA版）。
- 画面専用: ヒーロー、サマリーバー。
- 共通: TopMenuBar, PageHeader, SearchSection, AdvancedSearchPanel, ContextHealthAlert, Pagination。

## ワイヤーフレーム（案）
```text
┌──────────────────────────────────────────────────────────────┐
│ TopMenuBar  [≡] Logo  [SearchInput…………………] [+作成] [通知] [👤] │
│ (mobile: 2段目に SearchInput を全幅配置)                        │
├──────────────────────────────────────────────────────────────┤
│ ServiceHealthBar (any outage の場合のみ)                       │
├──────────────────────────────────────────────────────────────┤
│ HERO                                                        │
│  - H1: AIで演習問題を自動生成                                  │
│  - CTA: [問題を作成する] → /problem-create                     │
├──────────────────────────────────────────────────────────────┤
│ AdvancedSearchPanel (collapsible)                            │
│  [詳細条件を表示▼]                                            │
│   keyword / university / faculty / subject / teacher / year… │
├──────────────────────────────────────────────────────────────┤
│ SortTabs: おすすめ | 最新 | 閲覧数 | 高評価        件数表示        │
├──────────────────────────────────────────────────────────────┤
│ ContextHealthAlert (search degraded/outage/maintenance)      │
├──────────────────────────────────────────────────────────────┤
│ ResultsGrid                                                  │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐      │
│  │ SearchResult  │  │ SearchResult  │  │ SearchResult  │ ...  │
│  └───────────────┘  └───────────────┘  └───────────────┘      │
├──────────────────────────────────────────────────────────────┤
│ Pagination                                                   │
└──────────────────────────────────────────────────────────────┘
```

## ブロック → コンポーネント分割（案）
- Header: `Common/TopMenuBar.tsx`, `Common/ServiceHealthBar.tsx`
- Hero: `HomePage/HeroSection.tsx`
- Filters: `HomePage/AdvancedSearchPanel.tsx`, `HomePage/SearchFiltersForm.tsx`
- Sort/Count: `HomePage/SortTabs.tsx`
- Alert: `Common/ContextHealthAlert.tsx`
- Results: `HomePage/SearchResultsGrid.tsx`, `HomePage/SearchResultCard.tsx`, `Common/EmptyState.tsx`
- Pagination: `Common/Pagination.tsx`

## Sources
- `../implementation/pages/home-page.md`
- `../overview/requirements.md`, `../overview/current_implementation.md`
- `../implementation/service-health/README.md`
- `../implementation/visual/layout-guide.md`
