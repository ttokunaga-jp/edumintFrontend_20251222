import { useState } from 'react';
import type { User } from '@/types'; export const useRegisterForm = (onRegister: (user: User, isNewUser: boolean) => void) => { const [email, setEmail] = useState(''); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [isSubmitting, setIsSubmitting] = useState(false); const submit = async () => { setIsSubmitting(true); const now = new Date().toISOString(); const user: User = { id: crypto.randomUUID(), username: username || email || 'new_user', displayName: username || 'New User', email: email || 'new@example.com', isEmailVerified: false, universityName: 'EduMint University', university: 'EduMint University', facultyName: 'General Studies', department: 'General Studies', academicField: 'interdisciplinary', majorType: 'science', role: 'user', status: 'active', mintcoinBalance: 0, followerCount: 0, blockedCount: 0, createdAt: now, updatedAt: now, }; onRegister(user, true); setIsSubmitting(false); }; return { email, username, password, isSubmitting, setEmail, setUsername, setPassword, submit, };
};
